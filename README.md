### Параметры проекта Symfony Dotenv
+ Скопируйте `.env.example` в `.env`
+ Заполните необходимы переменные
### Сбор стилей Symfony Encore
+ dev-server: "encore dev-server"
+ dev: "encore dev"
+ watch: "encore dev --watch"
+ build: "encore production --progress"
